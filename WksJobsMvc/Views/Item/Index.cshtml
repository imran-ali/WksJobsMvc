@model IEnumerable<WksJobsMvc.Models.Item>

@{
    ViewData["Title"] = "Items list";
}

<h4>@ViewData["Title"]</h4>

<p>
    <a class="btn btn-sm btn-primary" asp-action="Create">Create new item</a>
</p>
@* <table class="table table-sm">
    <thead class="table-light">
        <tr>
            <th class="text-center">S.No</th>
            <th>
                @Html.DisplayNameFor(model => model.ItemCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitOfMeasurement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaterialDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PerUnitCost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Select((x,i) => new {Data=x, Index=i+1})) {
        <tr>
                <td class="text-center">@item.Index</td>
            <td>
                @Html.DisplayFor(modelItem => item.Data.ItemCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data.UnitOfMeasurement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data.MaterialDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data.PerUnitCost)
            </td>
            <td>
                    <div class="btn-group btn-group-sm" role="group" aria-label="Button group with nested dropdown">
                        <a asp-action="Edit" asp-route-id="@item.Data.Id" class="btn  btn-success"><i class="fa-sharp fa-solid fa-pen-to-square"></i> Edit Item</a>
                        <div class="btn-group btn-group-sm" role="group">
                            <button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                Action
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                <li><a class="dropdown-item" asp-action="Details" asp-route-id="@item.Data.Id">Details</a></li>
                                <li><a class="dropdown-item" asp-action="Delete" asp-route-id="@item.Data.Id">Delete</a></li>
                            </ul>
                        </div>
                    </div>
            </td>
        </tr>
}
    </tbody>
</table>
 *@


<table id="customerDatatable" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0">
    <thead>
        <tr>
            <th>ID</th>
            <th>Item code</th>
            <th>Material Description</th>
            <th>Per unit cost</th>
            <th>Unit of measurement</th>
            <th>Actions</th>
        </tr>
    </thead>
</table>


                    @section Scripts {
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css">
    <script type="text/javascript" language="javascript" src="https://code.jquery.com/jquery-3.7.0.js"></script>
    <script type="text/javascript" language="javascript" src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="~/js/item.js"></script>
                    }